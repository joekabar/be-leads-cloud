# Plan: Scaffold the be-leads repository (prompt 1)
Date: 2026-05-10
Author: Claude Code
Status: approved

## Goal
Scaffold the be-leads repository (prompt 1) so subsequent prompts can add skills and sources
within enforced TDD/plan-first guardrails. The result is a coherent Python project with working
CI tooling, a Postgres-backed docker compose environment, and hook-enforced conventions.

## Scope (in)
- All 30 files listed in the prompt 1 specification (root config, .claude/, agent_docs/, docs/,
  src/scraper/__init__.py, tests/ scaffold)

## Out of scope
- Source modules (src/scraper/sources/, db/, pipeline/, scoring/, lib/, ui/)
- Skills (.claude/skills/<name>/SKILL.md)
- Database migrations (prompt 3)
- Playwright browser installation
- git init / initial commit (user does this)

## Files to be created or modified
- pyproject.toml
- .python-version
- .gitignore
- .env.example
- README.md
- CLAUDE.md
- CHANGELOG.md
- Makefile
- docker-compose.yml
- .pre-commit-config.yaml
- .mcp.json
- .claude/settings.json
- .claude/settings.local.json.example
- .claude/plans/_template.md
- .claude/plans/2026-05-10-scaffold.md
- .claude/hooks/tdd_gate.sh
- .claude/hooks/format_and_test.sh
- .claude/hooks/coverage_gate.sh
- .claude/hooks/bash_safety.sh
- .claude/skills/.gitkeep
- .claude/commands/.gitkeep
- .claude/agents/.gitkeep
- agent_docs/architecture.md
- agent_docs/data-model.md
- agent_docs/runbook.md
- docs/decisions/0001-postgres-only.md
- src/scraper/__init__.py
- tests/__init__.py
- tests/conftest.py
- tests/test_scaffold.py

## Tests required (red first)
- tests/test_scaffold.py: `test_package_metadata` — imports `scraper` and asserts
  `scraper.__version__ == "0.1.0"`

## Acceptance criteria
- [ ] `uv sync --locked --dev` succeeds
- [ ] `pytest` collects ≥1 test and it passes
- [ ] `docker compose config` parses without errors
- [ ] `ruff check .` and `ruff format --check .` are clean
- [ ] `mypy src/scraper` is clean under `--strict`
- [ ] SessionStart hook produces a line in `.claude/hooks.log` on next session start

## Risks
- uv version may not support [dependency-groups] syntax (requires uv >=0.4)
- Hook scripts require bash + jq on the PATH; on Windows, Git Bash or WSL is needed

## Rollback plan
- Delete the directory and re-run the scaffold prompt
